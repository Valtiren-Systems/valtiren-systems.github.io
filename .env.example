# Postgres — matches the service defined in docker-compose.yml
DATABASE_URL="postgresql://valtiren:valtiren@localhost:5432/valtiren?schema=public"

# Public site URL used for metadata / canonical tags
NEXT_PUBLIC_SITE_URL="http://localhost:3000"
